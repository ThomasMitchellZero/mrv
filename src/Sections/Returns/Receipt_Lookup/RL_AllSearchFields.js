

const RLallSearchFields = () => {

  const StandardComponent = ({
    searchKey,
    matchFunc,
    validityFunc,
    fieldLabel,
    fieldPlaceholder,
  }) => {
    return <form className={classes.container}>

    </form>                    
  };

  const OneLineField = ()=>{

  }

  const makeObj = () => {};
};
