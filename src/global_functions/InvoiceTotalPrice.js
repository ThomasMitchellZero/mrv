

const invoicePriceSum = (invoiceObj) => {
        
    for(const thisInvo of Object.keys(invoiceObj)){
        let totalPaid = 0;

        for(const thisProduct of Object.keys(thisInvo)){
            
        }
    }
};


export default invoicePriceSum;
